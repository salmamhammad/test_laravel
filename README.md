# test_laravel
 The branch contains: <br/>
   HTTP endpoint: GET /api/products,   GET /api/categories<br/>
   product search with filters : q, price_from, price_to, category_id (select name, in_stock ( select true/false), rating_from<br/>
   Sorting: sort parameter with valid values: price_asc, price_desc, rating_desc, newest.<br/>
   Pagination <br/>
